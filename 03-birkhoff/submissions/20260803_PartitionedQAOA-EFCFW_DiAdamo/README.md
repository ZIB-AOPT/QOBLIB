# Partitioned QAOA E-FCFW Submission

This folder contains benchmarking results for the Birkhoff decomposition problem using a quantum-classical hybrid approach combining **Partitioned QAOA** with the **Extended Fully-Corrective Frank-Wolfe (E-FCFW)** framework.

**Submitter:** Stephen DiAdamo (Qoro Quantum Ltd.)  
**Affiliation:** Qoro Quantum Ltd., in collaboration with E.ON  
**Date:** 2026-08-03  

---

## Algorithm Overview

Our approach is built upon the **Extended Fully-Corrective Frank-Wolfe (E-FCFW)** framework introduced by Pennington et al. (2025) for graph decompositions, adapted here for the Birkhoff matrix decomposition benchmark in QOBLIB.

### 1. E-FCFW Framework (Pennington et al., 2025)
The Extended Fully-Corrective Frank-Wolfe (E-FCFW) algorithm iteratively decomposes a target matrix $M$ into a weighted combination of matching/permutation matrices:
1. **Matching Sampling**: In each iteration $k$, a set of candidate sub-matchings is generated by a sampling subroutine (such as QAOA) operating on the current residual matrix $M_{\text{res}} = M - X_k$.
2. **Weight Optimization & Pruning**: Weights for all accumulated candidate matchings are optimized (via convex quadratic optimization), discarding zero-weight matchings to maintain a sparse representation.

### 2. Novel Contribution: Graph Partitioning (Partitioned QAOA)
To scale QAOA sampling to large-scale benchmark instances ($16\times 16$, $32\times 32$, $64\times 64$) on real quantum hardware without exceeding qubit count or device connectivity constraints, we introduce **Graph Partitioning via Divi**:
* **Sub-graph Decomposition**: The residual demand matrix is mapped to a weighted bipartite graph and partitioned into smaller sub-graphs (with a maximum threshold of edges/qubits per partition, e.g. $\le 80$ edges resulting in 30–34 qubits).
* **Subgraph Characterization & Parameter Validation**: Each sub-graph partition undergoes pre-execution characterization to compute spectral energy bounds, validate penalty terms ($\lambda_{\text{safe}}$), and set initial QAOA parameters ($\beta, \gamma$), ensuring high likelihood of valid matchings upon measurement.
* **Parallel QPU Execution**: Each partition QUBO sub-problem is compiled into a 1-layer ($p=1$) QAOA circuit and executed on IBM Quantum hardware (`ibm_marrakesh` / `ibm_pittsburgh`) via Qoro Cloud Services.
* **Beam-Search Aggregation**: Candidate partition measurement samples are recombined into valid global matchings using a beam-search aggregation strategy.
* **Edge-Swap Local Refinement**: Local edge-swap moves are applied post-aggregation to further enhance matching weights prior to inclusion in the candidate pool.

### 3. Exact Birkhoff Completion & Metadata
* **Exact Completion (QOBLIB Requirement)**: While the original paper evaluates E-FCFW for approximate graph decompositions under an error tolerance $\epsilon$, the QOBLIB `03-birkhoff` benchmark strictly requires a mathematically **exact Birkhoff decomposition** ($\sum_{i=1}^k w_i P_i = M$ with zero residual error).
  * After completing the 15 QAOA sampling rounds, any remaining residual matrix entries $M_{\text{res}} > 0$ are exact-completed using the classical Birkhoff algorithm via **Hungarian matching** (`scipy.optimize.linear_sum_assignment`).
  * This guarantees that the final output in `*_solution.json` is a 100% exact Birkhoff permutation decomposition (verified by `check_birkhoff.rs`).
* **Success Threshold Metric (`N/A`)**: `Success Threshold` is set to `N/A` in the summary CSV metadata, as the solver produces exact decompositions for all reported runs.

---

## Software & Hardware Stack

- **Divi** (Qoro Quantum): QAOA circuit construction, graph partitioning, compilation, and QPU execution.
- **Quantum Hardware**: Executed on IBM Quantum processors (`ibm_marrakesh` / `ibm_pittsburgh`) via Qoro Cloud Services.
- **SciPy**: Exact classical Hungarian matching (`scipy.optimize.linear_sum_assignment`) for residual completion.
- **NetworkX**: Graph representation and utilities.

---

## Instance Naming & Folder Structure

Each subfolder `B{N}_{NNZ}_{ID}` holds the result for one problem instance:
- `N`: Dimension of the $N \times N$ doubly stochastic matrix (e.g. 16, 32, 64).
- `NNZ`: Matrix density indicator (sparse instances use $N$; dense instances use $N^2$).
- `ID`: Instance identifier (e.g., 1, 6).

---

## References

- **Pennington, G., et al.** *"Boosting Sparsity in Graph Decompositions with QAOA Sampling."* arXiv preprint [arXiv:2509.10657](https://arxiv.org/abs/2509.10657) (2025).
- **Koch, A., et al.** *"Quantum Optimization Benchmark Library (QOBLIB)."* arXiv preprint [arXiv:2504.03832](https://arxiv.org/abs/2504.03832) (2025).
