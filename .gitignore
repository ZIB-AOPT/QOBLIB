__pycache__
_site/

# Python tooling virtual environments (uv). The lockfile (misc/uv.lock) is committed.
.venv/
*.egg-info/

# Ignore Rust target directory
target/

# Environment / secret files — never commit these
.env
.env.*
*.local
